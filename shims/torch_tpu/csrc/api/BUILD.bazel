load("@nanobind_bazel//:build_defs.bzl", "nanobind_extension")
load("@rules_cc//cc:cc_library.bzl", "cc_library")
load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_test")
load("@rules_python//python:defs.bzl", "py_binary", "py_library", "py_test")

package(default_visibility = ["//visibility:public"])

# torch_tpu's public tensor-buffer API, taken from the installed wheel's
# headers; the torch extension binds the symbols at import to the wheel's
# per-torch-version common library.
cc_library(
    name = "tensor_buffer",
    deps = [
        "//shims/torch:aten_headers",
        "@com_google_absl//absl/base:nullability",
        "@com_google_absl//absl/status",
        "@com_google_absl//absl/status:statusor",
        "@com_google_absl//absl/types:span",
        "@installed_torch_tpu//:api_headers",
        "@xla//xla/pjrt:pjrt_client",
    ],
)
